{
  "name": "@kompass/water",
  "version": "0.1.0",
  "private": true,
  "description": "water.kompass.berlin — water4all (move target)",
  "scripts": {
    "dev": "echo 'apps/water is reserved for water4all — populate after move.' && exit 1",
    "build": "echo 'apps/water is a placeholder — see README.md' && exit 1"
  }
}
