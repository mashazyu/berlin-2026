# apps/polit

**Hosts:** `polit.kompass.berlin`, `www.polit.kompass.berlin`

Communal projects / Behördenkommunikation. Both domains → same Vercel project, Root Directory `apps/polit`.

```bash
pnpm --filter @kompass/polit dev   # port 3002
```
